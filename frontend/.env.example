# Bu dosyayı .env olarak kopyala, gerekirse backend URL'ini değiştir.
# Codespaces'te backend'i forwarded port URL'i ile değiştirmen gerekebilir
# (örn. https://<codespace-adi>-8000.app.github.dev).
VITE_API_BASE_URL=http://localhost:8000
